public class math {
    public static void main(String[] args) {
        double x = 3.75;
        double y = -10;
        double k = 25;
        double R = 3.13;
        double z = Math.abs(y);
        double M = Math.max(x,y);
        double m = Math.min(x,y);
        double s = Math.sqrt(k); 
       double r = Math.round(R);//.ceil round up floor ronund down
        System.out.println(z+" "+M+" "+m+" "+s+" "+r);
    }
}
